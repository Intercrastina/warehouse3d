<%= error_messages_for @step %>

<%= form_for @step do |f| %>
	<%= f.label :order, "Order" %>
	<%= f.text_field :order %>
	<%= f.label :description, "Description" %>
	<%= f.text_area :description %>
	
	<%= f.submit "Save" %>

<% end %>